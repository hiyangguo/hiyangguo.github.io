language: node_js
node_js:
  - stable
install:
  - npm install
before_deploy:
  - npm run build
deploy:
  provider: pages
    skip_cleanup: true
    github_token: $GITHUB_TOKEN
    local_dir: public
    target_branch: master
    on:
      branch: dev