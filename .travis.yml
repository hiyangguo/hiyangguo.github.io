language: node_js

node_js:
  - stable

install:
  - npm install

cache:
  directories:
    - node_modules
    - public

script:
  - hexo g

after_script:
  - gulp cp
  - cd ./public
  - git init
  - git config user.name "Godfery"
  - git config user.email "hiyangguo@qq.com"
  - git add .
  - git commit -m "Update docs"
  - git push --force --quiet "https://${GITHUB_TOKEN}@${GH_REF}" master
env:
 global:
   - GH_REF: github.com:hiyangguo/hiyangguo.github.io.git
branches:
  only:
    - dev

